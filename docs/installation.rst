==================================================
Installation
==================================================

You can install ``camelbird`` with ``pip``.

.. code-block:: console

    $ pip install git+https://github.com/hildeweerts/camelbird.git